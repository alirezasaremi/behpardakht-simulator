# Optional. Required only when fake callback delivery must reach controlled local receiver.
# Comma-separated exact http(s) origins; empty or absent disables callbacks.
# Security-sensitive: list only hosts and ports you control. Never use production endpoints.
SIMULATOR_CALLBACK_ALLOWED_ORIGINS=http://127.0.0.1:4010
