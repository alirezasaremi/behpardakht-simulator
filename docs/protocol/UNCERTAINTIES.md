# Protocol uncertainties

This ledger prevents simulator invention. `DOCUMENTED` means direct v1.39 statement; `DERIVED` is narrow restatement; `SIMULATOR_DECISION` is local design; `UNSPECIFIED` must not be guessed.

| Classification | Item | Status / handling |
| --- | --- | --- |
| DOCUMENTED | High-level stack | Source names SOAP/XML over HTTP or HTTPS. |
| DOCUMENTED | Provider WSDL locations | Printed source page 12 lists test `https://pgw.dev.bpmellat.ir/pgwchannel/services/pgw?wsdl` and operational `https://bpm.shaparak.ir/pgwchannel/services/pgw?wsdl`. PDF does not embed WSDL schema. |
| UNSPECIFIED | SOAP namespace, SOAPAction, WSDL schema content, SOAP envelope, operation wrapper, element order, serialization, and transport headers | Goal 3 local profile must not be claimed as provider behavior. |
| UNSPECIFIED | Exact response-string grammar beyond illustrated Pay `ResCode, RefId` and response-code strings | Preserve documented identifiers; establish parsing compatibility only in later source-backed work. |
| UNSPECIFIED | Full required/optional semantics for every specialized-operation parameter | Specialized methods deferred; extract only when their Goal begins. |
| SIMULATOR_DECISION | Goal 3 local SOAP endpoint and profile | `POST /api/soap`, SOAP 1.1 envelope, operation local-name matching, local response wrapper/faults, 400 fault status, and content type are usability choices. See `SOAP_COMPATIBILITY.md`. |
| UNSPECIFIED | Exact provider behavior for malformed SOAP/XML, unsupported operation, missing XML field, oversized input, and unauthorized/mismatched callback domain | Goal 3 uses local transport faults. It maps none of these to a provider ResCode. |
| UNSPECIFIED | Provider response for duplicate Pay `orderId` | Source says `bpPayRequest` `orderId` must be unique and duplicate requests return an error; table 11 separately labels `41` duplicate request number. No source text ties `41` to duplicate Pay. Goal 3 enforces uniqueness internally and returns a local HTTP 409 SOAP Fault, not a Behpardakht ResCode. |
| SIMULATOR_DECISION | In-memory, replaceable transaction repository | Planned initial persistence; no database in initial scope. |
| SIMULATOR_DECISION | Callback allowlist, SSRF protections, timeout/response caps, and XML size/DTD/entity protections | Security baseline imposed by project, beyond documented merchant domain rule. |
| UNSPECIFIED | StartPay POST response/status/redirect mechanics, unknown RefId behavior, request body encoding, and handling of repeated submissions | v1.39 proves POST with RefId but not these response behaviors. Goal 4 uses bounded URL-encoded input, local 303, safe 404/400, and duplicate rejection as local behavior. |
| UNSPECIFIED | Callback request encoding/content type, callback response handling, timeout, redirect, retry, and transport-failure semantics | v1.39 proves POST fields but not transport profile. Goal 4 chooses URL-encoded body, no redirects/retries, three-second timeout, 16 KiB drain, and separate safe event diagnostics. |
| DOCUMENTED | StartPay optional field evidence | Printed pages 18-20 show `MobileNo`, `EncPan`, `HiddenMode`, `merchantName`, `merchantAddress`, `GamBonds`, and `SettleTime`; printed page 16 Pay table uses `mobileNo`, `encPan`, `panHiddenMode`. Do not infer casing equivalence. |
| SIMULATOR_DECISION | Fake CardHolderPan, blank CreditCardSaleResponseDetail, FinalAmount equals Pay amount, decimal SaleReferenceId generation | Goal 4 avoids real card information and does not implement discount/credit provider semantics or a provider identifier algorithm. |
| SIMULATOR_DECISION | Exact-origin allowlist DNS limitation | String-origin allowlisting cannot prevent a previously trusted hostname resolving to a different IP after validation; local simulator documents this residual risk instead of claiming full DNS-rebinding prevention. |
| SIMULATOR_DECISION | Clock, timer execution, state representation, idempotency mechanics, diagnostics, and UI | Required to simulate; source does not define implementation mechanism. |
| UNSPECIFIED | Exact payment-page UI/card data entry flow | Goal 4 page is explicitly fake/local, credential-free, and has fixed simulator outcome controls. It does not claim provider UI behavior. |
| DOCUMENTED | `SaleOrderId` correlation | Printed page 11 says Sale-stage `orderId` becomes `SaleOrderId`; printed page 33 says callback `OrderId` must exactly equal original Pay `OrderId` for same transaction. |
| DOCUMENTED | `encPan` table casing versus redirect examples using `EncPan`; `panHiddenMode` table versus `HiddenMode` redirect example | Preserve source evidence; do not normalize or infer a wire mapping without validation. |
| UNSPECIFIED | Full `bpReversalRequest` eligibility after Verify | Source says Reversal follows Verify for unknown payment, but does not specify successful Sale/callback or distinguish unknown versus successful Verify result. Goal 7 requires only local prior Verify invocation and does not claim full provider eligibility. |
| DOCUMENTED | Verify table-2 fields and non-unique Verify `orderId` | Printed page 21 names exact fields/types, describes `orderId` as verification request number, and permits equality with `saleOrderId`. Goal 5 retains all long values as `bigint`. |
| DOCUMENTED | Verify success/repeated verification | Printed page 21 says Verify returns a response-code string and retry wording includes success/previously verified; table 11 code `0` is success and `43` says prior Verify succeeded. Goal 5 returns only these codes. |
| UNSPECIFIED | Verify-specific response for unknown/mismatching Sale correlation | Table 11's code `42` matching-Sale explanation is explicit for refund, not Verify. Goal 5 uses local SOAP fault, no provider-code inference. |
| UNSPECIFIED | Verify credential authentication semantics | Table 2 lists `terminalId`, `userName`, and `userPassword`, but source does not make enough local acceptance/credential-storage rules. Goal 5 accepts fake test values and never persists/logs password. |
| UNSPECIFIED | Verify result after 20-minute automatic reversal absent automatic lifecycle state | Page 21 describes automatic reversal and retry may return already reversed; no timer/reversal execution exists. Goal 7 returns `48` only for internal known-reversed state, never from local `bpReversalRequest`. |
| DOCUMENTED | Settle table-3 fields, request identifiers, and success | Printed page 22 names exact fields/types. Settle `orderId` is settlement-request number, non-unique, and may equal `saleOrderId`; `0` means settlement request received successfully. Goal 6 preserves long values as `bigint`. |
| UNSPECIFIED | Settle-specific nonzero results for repeat, pre-Verify, unknown, or mismatched correlation | Table 11 has settlement-related `45`/`46`/`47`/`61`, but printed page 22 does not tie them to `bpSettleRequest`. Goal 6 returns local SOAP Fault without mutation, never speculative provider code. |
| UNSPECIFIED | Relationship between merchant-requested Settle, automatic three-hour/six-hour settlement, and eventual merchant deposit | Printed page 20 describes automatic timing and `SettleTime`; page 22 describes merchant Settle and bank/deposit wording. No source rule reconciles timing, request acceptance, lifecycle, and banking completion. Goal 6 records accepted request only; no timer/deposit model. |
| UNSPECIFIED | Settle credential authentication semantics | Table 3 lists terminal/user/password but provides no simulator-local credential acceptance/storage behavior. Goal 6 uses fake structural input and never persists/logs password. |
| DOCUMENTED | Inquiry table-4 contract and request-number semantics | Printed page 23 names fields/types, purpose, response-code return, non-unique Inquiry `orderId`, and permitted equality with `saleOrderId`. Goal 7 retains `long` as `bigint`. |
| UNSPECIFIED | Inquiry-specific response codes for verified, settled, reversed, unknown, or mismatching transaction | Page 23 says response-code string but does not map those states to codes. Table-11 `0` alone is insufficient; Goal 7 faults locally without provider code. |
| UNSPECIFIED | Inquiry credential authentication semantics | Table 4 lists terminal/user/password but does not establish local credential acceptance/storage behavior. Goal 7 accepts fake structural input and never persists/logs password. |
| DOCUMENTED | Reversal table-5 contract and request-number semantics | Printed pages 23-24 name fields/types, purpose, response-code return, non-unique Reversal `orderId`, and permitted equality with `saleOrderId`. Goal 7 retains `long` as `bigint`. |
| UNSPECIFIED | Reversal-specific repeated-call/unknown/mismatching response codes | Table 11's `48` global meaning does not tie code to repeated `bpReversalRequest`. Goal 7 faults locally without mutation. |
| UNSPECIFIED | Reversal-specific success result and state effect | Pages 23-24 name response-code string but do not identify `0`, request acceptance, or completed reversal. Table-11 `0` alone is insufficient; Goal 7 faults locally without mutation. |
| DERIVED | Settlement-requested to Reversal local rejection | Page 24's end-of-day condition requires no settlement request. Goal 7 conservatively rejects local Reversal after settlement; source does not explicitly define response/result. |
| UNSPECIFIED | Reversed transaction to later Settle eligibility | Source condition is one direction only. Goal 7 does not enforce inverse `REVERSED -> Settle` rule. |
| UNSPECIFIED | Relationship between Reversal three-hour-after-Verify and end-of-current-day/no-settlement statements | Printed page 23 states three hours after Verify; page 24 separately states end of current day if no settlement request. Goal 7 records both and enforces neither deadline. |
| UNSPECIFIED | Reversal credential authentication semantics | Table 5 lists terminal/user/password but does not establish local credential acceptance/storage behavior. Goal 7 accepts fake structural input and never persists/logs password. |

| DOCUMENTED | VerifySettle contract, request-number semantics, and known-state results | Printed pages 31-32 table 12 names fields/types; `orderId` need not be unique and may equal `saleOrderId`. Page 32 names VerifySettle success and prior Verify/Settle/Reversal retry outcomes; table 11 maps them to `0`/`43`/`45`/`48`. |
| SIMULATOR_DECISION | Atomic VerifySettle lifecycle/event representation | Goal 8 uses one immutable save to mark verified plus settlement requested, with one `SETTLEMENT_REQUESTED` event marked `via: "VERIFY_SETTLE"`; no false separate merchant SOAP events. |
| UNSPECIFIED | VerifySettle after non-success Sale, unresolved separate Verify, missing callback, or callback transport failure | Pages 31-32 describe successful-Sale/callback workflow but do not map these states to a VerifySettle result. Goal 8 returns local no-mutation SOAP Fault. |
| UNSPECIFIED | VerifySettle credential authentication and exact wire grammar | Table 12 supplies fields/types but no local credential-acceptance, WSDL schema, envelope, or serialization rule. Fake credentials are structural input only and never persist/log. |
| UNSPECIFIED | VerifySettle 20-minute automatic-reversal execution | Page 32 states 20 minutes after successful Sale without VerifySettle triggers automatic reversal request and returned funds, but gives no simulator mechanism. Goal 8/9 implement no timer, worker, request-time reversal, or automatic-reversal scenario. |
| UNSPECIFIED | Universal unresolved `bpVerifyRequest` numeric result | Page 21 directs retry after nonzero callback result but does not tie operation-specific provider ResCode to unresolved Verify attempt. Goal 9 uses `SIMULATOR_SCENARIO` local Fault `SimulatorScenario.VerifyUnresolved`, never numeric provider result. |
| SIMULATOR_SCENARIO | Deterministic scenario registry/control | Goal 9 `NORMAL`, `VERIFY_UNRESOLVED`, `KNOWN_REVERSED`, RefId-scoped `/local/api/scenarios`, eligibility checks, process-memory lifetime, clear behavior, `SCENARIO_STATE_FORCED` test controls, not Behpardakht behavior. |
| SIMULATOR_SCENARIO | Forced known reversed state | Goal 9 may set eligible successful local Sale `REVERSED` without `bpReversalRequest`; this creates source-backed later Verify/VerifySettle `48` only because operation-specific mappings already exist. |
| UNSPECIFIED | Provider HTTP failure, malformed SOAP/XML, delay, timeout, and connection abort behavior | v1.39 establishes HTTP(S)/SOAP but not these behavior details. Goal 10 uses bounded simulator-only HTTP 503, fixed malformed fixture, and fixed 250 ms delay; genuine abort deferred. |
| SIMULATOR_SCENARIO | Goal 10 transport-fault control | Separate RefId-scoped, target-operation, one-shot `/local/api/transport-faults`; PRE prevents execution, POST follows normal committed handler result. No arbitrary status/body/XML/delay/header/socket scripting. |
| DOCUMENTED | Goal 12 remaining-operation inventory | `docs/protocol/REMAINING_PROTOCOL_AUDIT.md` records direct evidence for Charge, Refund variants, Dynamic Pay variants, redirect-specialized fields, timings, and absent refund inquiry contract. It is based only on v1.39 printed pages 9-37. |
| UNSPECIFIED | Dynamic Pay nonzero result mappings and provider `subServiceId` validation | Printed pages 28-29/table 9 show request fields, unique request number, `0,RefId`, and next-stage RefId flow; no Dynamic-Pay-specific nonzero code/validation mapping. Goal 12 returns only source-backed normal success. |
| SIMULATOR_DECISION | Safe Pay-family optional-field boundary | `bpPayRequest`, `bpDynamicPayRequest`, and `bpCumulativeDynamicPayRequest` reject `mobileNo`, `encPan`, and `enc` to avoid personal/card/identity data intake; safe optional display values are discarded. This is not a provider acceptance claim. |
| UNSPECIFIED | Provider cross-operation Pay/Dynamic Pay order-number collision | Source requires uniqueness for each named request but does not say whether same terminal/order is invalid across Pay and Dynamic Pay. Goal 12 preserves terminal-wide local uniqueness as `SIMULATOR_INTERNAL` ambiguity prevention and returns local operation-specific SOAP Faults, never provider `41`. |
| UNSPECIFIED | Refund final state and inquiry contract | Printed pages 25-27 say Refund `0` is initial acceptance and direct merchant to separately documented refund-inquiry services. That contract is not in supplied v1.39, so no final refund lifecycle/result is simulated. |
| DOCUMENTED | Cumulative Dynamic Pay request shape and normal result | Printed pages 10 and 30-31/table 10 establish operation name, fields/types, at-most-ten comma/semicolon account/amount/payer triples, total-sum requirement, `0,RefId`, RefId POST, unique request number, and high-level Pay-like later workflow. |
| SIMULATOR_DECISION | Goal 13 Cumulative Dynamic Pay normal parser | Local parser accepts table-10 delimiter normal path, illustrated empty payer-id/terminal semicolon, ASCII decimal bigint distribution amounts, and sum equality. It discards distributions/account/payer data, uses existing Pay-family transaction state, and reports unsupported lexical cases with local Faults. |
| UNSPECIFIED | Cumulative Dynamic Pay provider validation and finality | v1.39 does not define account authorization/provisioning, account/payer lexical rules, negative/zero amount policy, nonzero result mappings, retry/timing, payout execution, settlement completion, or exact WSDL wire grammar. |

Add discoveries with source page and classification before code depends on them.
